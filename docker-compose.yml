version: "3.8"
services:
  arangodb:
    image: arangodb:latest
    environment:
      - ARANGO_ROOT_PASSWORD=local
    ports:
      - "8529:8529"
    volumes:
      - arangodata:/var/lib/arangodb3

volumes:
  arangodata:
